import React from 'react';
import { useEffect } from 'react';

export default function Skills() {
  useEffect(() => {
    window.scrollTo(0, 0);
  }, []);

  return (
    <div>
      <h1>Skills</h1>
    </div>
  );
}
